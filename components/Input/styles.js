import { StyleSheet } from "react-native";
import colors from "../constants/colors.js";

const styles = StyleSheet.create({
  input: {
    backgroundColor: colors.blCiel,
    paddingHorizontal: 24,
    paddingVertical: 13,
    borderRadius: 10,
    marginVertical: 12,
    color: colors.primary,
    fontSize: 15,
  },
});

export default styles;
